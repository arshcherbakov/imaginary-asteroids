import { TableCell } from "@mui/material";
import styled from "styled-components";

const StyledTableApproachCell = styled(TableCell)(() => ({
  display: "flex",
  justifyContent: "space-evenly",
  alignItems: "center",
}));

export { StyledTableApproachCell };
