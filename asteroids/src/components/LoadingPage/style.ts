import { Box } from '@mui/material';
import styled from 'styled-components';

const StyledCircularProgressWrapper = styled(Box)(() => ({
  marginTop: '100px',
  display: 'flex',
  justifyContent: 'center',
}));

export { StyledCircularProgressWrapper };
